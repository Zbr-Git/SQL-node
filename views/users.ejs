<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Users</title>
    <!-- Include Tailwind CSS stylesheet -->
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>

  <body
    class="bg-gray-100 min-h-screen flex flex-col justify-center items-center py-12"
  >
    <div
      class="max-w-4xl mx-auto bg-white shadow-md rounded-lg overflow-hidden"
    >
      <div class="bg-gray-800 text-white py-4 px-6">
        <h2 class="text-2xl font-bold">List of all users</h2>
      </div>
      <div class="mt-4 flex justify-end">
        <form method="GET" action="/user/new">
          <button
            class="bg-green-500 hover:bg-green-600 text-white font-bold py-2 px-4 rounded mx-6"
          >
            Add New User
          </button>
        </form>
      </div>
      <div class="p-6">
        <table class="w-full">
          <thead>
            <tr class="bg-gray-200">
              <th class="py-2 px-4 border border-gray-300">Id</th>
              <th class="py-2 px-4 border border-gray-300">Email</th>
              <th class="py-2 px-4 border border-gray-300">Username</th>
              <th class="py-2 px-4 border border-gray-300">Actions</th>
            </tr>
          </thead>
          <tbody>
            <% for(user of data) { %>
            <tr class="hover:bg-gray-100 transition-colors">
              <td class="py-2 px-4 border border-gray-300"><%= user.id %></td>
              <td class="py-2 px-4 border border-gray-300">
                <%= user.email %>
              </td>
              <td class="py-2 px-4 border border-gray-300">
                <%= user.username %>
              </td>
              <td class="py-2 px-4 border border-gray-300 flex justify-center">
                <form method="get" action="/user/<%=user.id%>/edit">
                  <button
                    class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-1 px-2 rounded"
                  >
                    Edit
                  </button>
                </form>
                <form method="get" action="/user/<%=user.id%>/delete">
                  <button
                    class="bg-red-500 hover:bg-red-600 text-white font-bold py-1 px-2 rounded ml-2"
                  >
                    Delete
                  </button>
                </form>
              </td>
            </tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </div>
  </body>
</html>
